# Virtual Head セキュリティ方針

最終更新：2026-09-08  
管理者：tatto

## 脆弱性の連絡

公開後は、公開リポジトリの GitHub Security Advisories から非公開で報告してください。公開Issueへ、未修正の脆弱性、個人情報、悪用手順を投稿しないでください。公開前の試用版については、配布者のtattoへ直接連絡してください。

報告には、Virtual Headのバージョン、Windowsのバージョン、再現手順、影響、関連するログを含めてください。ログを送る前にローカルのユーザー名やファイルパスを確認してください。受領確認、影響判断、修正版の予定は、連絡手段が確定した公開版で改めて明記します。

## 安全な利用

- アプリは管理者として実行しないでください。
- ONNXモデルはコードではありませんが、ONNX Runtimeが解析してネイティブ処理を実行します。信頼できる提供元のモデルだけを選択してください。
- 登録済みモデルは `MODEL_HASHES.json` のサイズとSHA-256で照合します。同名で値が異なるファイルは拒否します。未登録の自作モデルは実行できますが、`run.json`へ `unlisted-user-model` と記録します。
- FFmpegは選択した実行ファイルを起動します。公式配布元などから入手した信頼できる `ffmpeg.exe` だけを指定してください。
- CUDA DLLフォルダにはプロセスへ読み込まれるDLLが置かれます。NVIDIAの正規インストール先など、信頼できるフォルダだけを指定してください。
- 画像、動画、Skinは最新のWindows Defenderなどで検査してください。Virtual Head側でも容量、寸法、時間の上限を検査します。

## 実装済みの防御

- 静止画：128 MiB、長辺16,384px、5,000万画素まで。OpenCVで展開する前にPillowでヘッダーを検査します。
- Skin：PNGのみ、8 MiB、長辺1,024pxまで。Minecraft Skinの寸法規則も続けて検査します。
- 動画：50 GiB、12時間、長辺8,192px、1フレーム5,000万画素まで。メタデータと実フレームの両方を検査します。
- ONNX：1ファイル4 GiBまで。既知ファイル名はSHA-256を照合します。
- 外部プログラムの起動にはシェルを使わず、引数配列でFFmpegを実行します。
- アプリは通常ユーザー権限での実行を前提とします。

これらの上限は安全性と一般的な用途の両立を目的としたものです。変更する場合は、メモリ使用量と異常入力の試験を行います。

## 配布時の確認

一般公開する成果物は、クリーンなビルド環境で作り、テスト、依存関係監査、マルウェア検査、SHA-256生成を行います。正式版ではコード署名を行い、署名済みMSIXまたは署名済み実行ファイルを配布します。ソース用リポジトリとバイナリ公開用リポジトリを分離します。
