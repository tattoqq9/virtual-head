# モデル選択UIの設計

Virtual Head 0.1.0-alpha.12では、頭部検出モデルと回転角モデルを別々のドロップダウンから選びます。名前の付いた全プリセットをUIから取得でき、ONNXファイルを直接指定する欄も残しています。

## 頭部検出モデル

| 画面上の選択肢 | 速度・用途 | 推奨環境 | 取得方法 |
|---|---|---|---|
| 自動 | 入手済みモデルから推奨順に選択 | 初めて使う場合 | ローカル検索 |
| おすすめ：Gold-YOLO-N 192×320 | 今回の実動画で高速かつ追従良好 | CPU / DirectML GPU | UIから公式アーカイブを取得・抽出 |
| 標準：YOLOv9-t 640×640 | 小さいファイルで導入しやすい標準構成 | CPU / DirectML GPU | UIから個別／標準セット取得 |
| 精度重視：DEIMv2 DINOv3-S | 速度より検出品質を比較する構成 | GPU推奨 | UIから個別取得 |
| 最高品質候補：DEIMv2 DINOv3-X | 最初の検証で使用した大型構成 | 高性能GPU推奨 | UIから個別取得 |
| カスタムONNX | 自作・互換モデルの検証 | モデルによる | ローカルファイルを選択 |

「自動」はGold、YOLOv9-t、DEIMv2-S、DEIMv2-Xの順に、PC上で見つかった最初のモデルを使用します。明示的にモデルを選んだ場合は、ファイルがなくても別のモデルへ自動置換しません。これにより、意図しない条件で比較や書き出しが行われることを防ぎます。

GoldとYOLOv9-tの速度は同じテスト動画で測定しました。DEIMv2の「精度重視」「最高品質候補」はモデル規模と元の利用目的を示す分類であり、異なるデータセット間の精度順位を保証する表現ではありません。

## 回転角モデル

| 画面上の選択肢 | 速度・用途 | 推奨環境 | 取得方法 |
|---|---|---|---|
| 自動 | 入手済みモデルからYawNet 128を優先 | 通常利用 | ローカル検索 |
| 標準・精度重視：YawNet 128 | 現在の標準 | CPU / DirectML GPU | UIから個別／標準セット取得 |
| 軽量：YawNet 64 | 入力解像度を抑えてCPU速度を優先 | CPU | UIから個別取得 |
| カスタムONNX | 自作・互換モデルの検証 | モデルによる | ローカルファイルを選択 |

頭部検出器を変更してもYawNetは独立して選択できます。軽量な検出器への変更がYawNet自体の推論精度を直接変更することはありません。ただし、検出枠の位置や大きさが変わるとYawNetへ渡すクロップ画像が変わるため、最終的な角度結果は影響を受ける可能性があります。

## 画面動作

- 選択肢の下に、速度、特性、推奨環境、説明、ファイル取得状況を表示します。
- 既知のファイル名を「選択」した場合、該当プリセットへ自動的に切り替えます。
- 旧バージョンの設定は、保存されたファイル名からプリセットへ一度だけ移行します。
- 「選択中の頭部モデルを取得」と「選択中の回転角モデルを取得」は、各ドロップダウンで選んだプリセットを公式配布元から取得し、その選択へ反映します。
- 「標準2モデルをまとめて取得」はYOLOv9-tとYawNet 128を一度に取得します。
- Gold-YOLO-Nの公式配布物は約1.37GBの圧縮アーカイブです。アプリは対象の22.5MB ONNXだけを展開しますが、通信量は最大約1.37GBになるため、確認画面に個別表示します。
- 取得前の確認画面にはモデル容量、最大通信量、保存先、権利確認の注意を表示します。
- 「選択モデルの公式ページ」と「ライセンス・情報」から公式情報を確認できます。
- CUDA DLL、任意ランドマーク、FFmpegは「詳細」タブへまとめます。

公開ZIPに第三者のONNXモデルは含めません。モデル選択の表示は、モデル重みの権利や利用許諾を付与・保証するものではありません。利用前に`MODEL_LICENSES.md`の公式確認先を開き、各自の用途で利用できるか判断してください。
